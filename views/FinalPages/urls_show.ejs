<!DOCTYPE html>
<html lang="en">
<head>
    <% include head %>
</head>
<body class="container">
<% if (userID) { %>
    <header>
        <% include _header %>
    </header>
    <main>
      <h2>Welcome to TinyApp Url Shorning Service!</h2>
      <table class="table table-bordered table-responsive">
        <thead>
          <tr>
            <th>Short URL</th>
            <th>Long URL</th>
            <th>Edit URL</th>
          </tr>
        </thead>
         <tr>
           <td><%= urlDatabase.shortUrl %></td>
           <td><%= urlDatabase.longUrl %></td>
           <td>
             <form action="/urls/<%= urlDatabase.shortUrl %>" method="post" >
               <label for="longUrl">Enter a URL:
                 <input id="longUrl" type="text" name="longUrl" placeholder="http://" style="width: 300px">
               <input type="submit" value="Submit" class="btn btn-warning btn-sm">
             </label>
             </form>
           </td>
         </tr>
      </table>
        <div class="jumbotron">
            <h2>This is great</h2>
            <p>Welcome to my Tiny App!</p>
        </div>
    </main>

    <footer>
        <% include footer %>
    </footer>
  <% }; %>
</body>
</html>
